/**
 * Interface representing a navigation link.
 */
export interface NavigationLink {
  /** Title of the navigation link. */
  title: string

  /** URL of the navigation link. */
  url: string

  /** Icon name of the navigation link. */
  iconName: string
}
