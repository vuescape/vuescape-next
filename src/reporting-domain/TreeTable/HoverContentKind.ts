/**
 * Enum representing the kind of content for a Hover.
 */
export enum HoverContentKind {
  /** No content. */
  None = 0,

  /** Plain text content. */
  Plaintext,

  /** HTML content. */
  Html,
}
