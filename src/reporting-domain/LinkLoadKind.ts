/**
 * Enum representing a LinkLoadKind.
 */
export enum LinkLoadKind {
  /** Represents no load kind. */
  None,

  /** Load link when created. */
  WhenCreated,

  /** Load link when mounted. */
  WhenMounted,
}
