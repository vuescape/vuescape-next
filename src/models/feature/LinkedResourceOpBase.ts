/**
 * Interface representing a base operation for linked resources.
 */
export interface LinkedResourceOpBase {
  /** Object containing the URL of the linked resource. */
  linkedResource: { url: string }
}
