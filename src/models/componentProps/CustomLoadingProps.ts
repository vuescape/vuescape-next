/**
 * Interface for CustomLoading component props.
 */
export interface CustomLoadingProps {
  /**
   * Determines whether the loading indicator is visible.
   * @default false
   */
  isVisible: boolean
}
