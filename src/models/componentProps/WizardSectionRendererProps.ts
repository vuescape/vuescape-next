import type { PaneSection } from '../dynamic-ui/PaneSection'

/**
 * Interface representing the properties required for rendering an array of pane components in a wizard.
 */
export interface WizardSectionRendererProps {
  section: PaneSection
}
