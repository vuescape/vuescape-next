/**
 * Represents the possible positions for an icon within a component.
 * 
 * @typedef {('left' | 'right' | 'top' | 'bottom')} IconPosition
 */
export type IconPosition = 'left' | 'right' | 'top' | 'bottom'
