import type { ActionButtonProps } from '../../componentProps/ActionButtonProps'

/**
 * Represents the payload for an action button component in the dynamic UI.
 */
export type ActionButtonComponentPayload = ActionButtonProps
