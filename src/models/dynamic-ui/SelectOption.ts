/**
 * Represents the layout of a pane, including its sections.
 */
export interface SelectOption {
  /**
   * The unique identifier for the select option.
   */
  id: string

  /**
   * The value to display for the select option.
   */
  displayName: string
}
