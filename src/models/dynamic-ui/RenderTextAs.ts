/**
 * Represents the possible ways to render text as in the dynamic UI.
 *
 * - `'heading'`: A main heading or title.
 * - `'subheading'`: A secondary heading or subtitle.
 * - `'paragraph'`: A standard block of text.
 * - `'html'`: HTML.
 */
export type RenderTextAs = 'heading' | 'subheading' | 'paragraph' | 'html'
