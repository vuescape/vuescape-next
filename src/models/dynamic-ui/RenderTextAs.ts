/**
 * Represents the possible ways to render text as in the dynamic UI.
 *
 * - `'header'`: A main heading or title.
 * - `'subheader'`: A secondary heading or subtitle.
 * - `'paragraph'`: A standard block of text.
 * - `'html'`: HTML.
 */
export type RenderTextAs = 'header' | 'subheader' | 'paragraph' | 'html'
